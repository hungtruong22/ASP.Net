@model SV20T1020390.web.Models.OrderDetailModel
@{
    Layout = null;
}

<div class="modal-dialog">
    <div class="modal-content">
        <form id="formChangeAddress" action="~/Order/ChangeAddress/@ViewBag.OrderID" method="post">
            <div class="modal-header bg-primary">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Cập nhật địa chỉ</h4>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    <label class="control-label">Nhập địa chỉ giao hàng:</label>
                    <input type="text" class="form-control" name="@nameof(Model.Order.DeliveryAddress)">
                </div>
                <div class="form-group">
                    <div class="col-md-offset-3 col-sm-9">
                        <span id="message" style="color: #f00"></span>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-primary">
                    <i class="fa fa-floppy-o"></i> Cập nhật
                </button>
                <button type="button" class="btn btn-default" data-dismiss="modal">
                    Bỏ qua
                </button>
            </div>
        </form>
    </div>
</div>
<script>
    $(document).ready(function () {
        $('#formChangeAddress').submit(function (e) {
            e.preventDefault();
            var url = $(this).prop("action");
            var method = $(this).prop("method");
            var postData = $(this).serializeArray();

            $.ajax({
                url: url,
                type: method,
                data: postData,
                error: function () {
                    alert("Your request is not valid!");
                },
                success: function (data) {
                    if (data !== "") {
                        $("#message").html(data);
                    }
                    else {
                        location.reload();
                    }
                }
            });
            return;
        });
    });
</script>
